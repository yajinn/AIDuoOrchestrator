{
  "name": "ai-duo-orchestrator",
  "description": "Terminal-first protocol and runtime for Claude and Codex pair workflows.",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "main": "./dist/orchestrator.js",
  "bin": {
    "aiduo": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "PRD-v3-terminal.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist coverage .vitest",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  }
}
