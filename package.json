{
  "name": "aiduo",
  "description": "Terminal-first protocol and runtime for Claude and Codex pair workflows.",
  "version": "0.1.1",
  "license": "UNLICENSED",
  "main": "./dist/orchestrator.js",
  "bin": {
    "aiduo": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yajinn/AIDuoOrchestrator.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist coverage .vitest",
    "lint": "tsc --noEmit -p tsconfig.json",
    "prepare": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  }
}
